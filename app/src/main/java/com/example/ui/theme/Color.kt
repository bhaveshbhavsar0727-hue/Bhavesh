package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish Theme Colors
val ProfBlue = Color(0xFF005CB9)         // Primary professional blue (logo, buttons, primary actions)
val IceBlueBg = Color(0xFFF8F9FF)        // Premium soft ice-blue background
val InkDark = Color(0xFF1B1B1F)          // Deep professional ink dark text/onBackground
val LightPillBg = Color(0xFFD3E3FD)      // Selected bottom bar item pill, highlighted containers
val DeepNavyText = Color(0xFF001C38)     // High-contrast deep navy text for selected pills
val AccentBlueCard = Color(0xFFE8F0FE)   // Info/Confidence cards
val AccentGrayCard = Color(0xFFF2F0F4)   // Neutral slate accent card
val DarkTipBg = Color(0xFF1D1B20)        // Dark Charcoal card background (e.g. Tips)
val SoftBorder = Color(0xFFE2E8F0)       // Subtle borders around white cards (slate-100/200 style)

// Semantic mapping colors for the app to avoid unresolved references
val CalmingTeal = Color(0xFF005CB9)      // Primary reassuring accent (mapped to ProfBlue for consistency)
val DeepSlate = Color(0xFF1B1B1F)         // InkDark (used for onBackground, onSurface)
val SoftSlate = Color(0xFF334155)         // Secondary borders/surfaces
val PaleCream = Color(0xFFF8F9FF)         // Premium light background (IceBlueBg)
val WarmSand = Color(0xFFE2E8F0)          // Soft warm surface
val SoftOliveText = Color(0xFF475569)     // Secondary calming text color
val MistGreen = Color(0xFFE8F0FE)         // Primary container color (mapped to AccentBlueCard)
val CalmingGreenLight = Color(0xFFE2F0EC) // Super soft green card back
val AnxietyRed = Color(0xFFEF4444)        // Alert/anxious pitch indicators
val ReassuringYellow = Color(0xFFF59E0B)  // Highlights
